import {describe,expect,it} from 'vitest';import {diagnosticCommandSchema} from '../../electron/preload/contracts.js';describe('diagnostic contracts',()=>{it('accepts narrow commands',()=>expect(diagnosticCommandSchema.parse({type:'set_seed',seed:42}).seed).toBe(42));it('rejects arbitrary commands',()=>expect(()=>diagnosticCommandSchema.parse({type:'run_shell',value:'oops'})).toThrow())});
