import {describe,expect,it} from 'vitest';import {EventTraceBuffer,TraceReplayer} from '../../packages/pet-engine/diagnostics/src/index.js';
describe('diagnostic traces',()=>{it('bounds and exports events',()=>{const b=new EventTraceBuffer(2,7);b.record({monotonicMs:1,category:'system',name:'one'});b.record({monotonicMs:2,category:'system',name:'two'});b.record({monotonicMs:3,category:'system',name:'three'});expect(b.snapshot().map(x=>x.name)).toEqual(['two','three']);expect(b.exportTrace().seed).toBe(7)});it('replays only commands',async()=>{const applied:string[]=[];const r=new TraceReplayer();const count=await r.replay({format:'pokoloko-diagnostic-trace',version:1,exportedAtIso:'x',seed:1,events:[{sequence:1,monotonicMs:1,category:'diagnostic',severity:'info',name:'x',replayCommand:{type:'force_idle'}}]},async c=>{applied.push(c.type)});expect(count).toBe(1);expect(applied).toEqual(['force_idle'])})});
